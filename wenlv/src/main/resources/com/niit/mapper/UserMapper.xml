<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.niit.mapper.UserMapper">

    <resultMap id="UserResultMap" type="com.niit.pojo.User">
        <id property="id" column="id"/>
        <result property="phone" column="phone"/>
        <result property="email" column="email"/>
        <result property="password" column="password"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <select id="findByPhoneOrEmail" resultMap="UserResultMap">
        SELECT id, phone, email, password, create_time
        FROM user
        WHERE phone = #{phoneOrEmail} OR email = #{phoneOrEmail}
    </select>

    <select id="findByEmail" resultMap="UserResultMap">
        SELECT id, phone, email, password, create_time
        FROM user
        WHERE email = #{email}
    </select>

    <select id="findByPhone" resultMap="UserResultMap">
        SELECT id, phone, email, password, create_time
        FROM user
        WHERE phone = #{phone}
    </select>

    <insert id="insertUser" parameterType="com.niit.pojo.User">
        INSERT INTO user (phone, email, password)
        VALUES (#{phone}, #{email}, #{password})
    </insert>

</mapper>