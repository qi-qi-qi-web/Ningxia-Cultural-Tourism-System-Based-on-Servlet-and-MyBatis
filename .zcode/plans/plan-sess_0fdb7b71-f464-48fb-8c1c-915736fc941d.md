## 评论分类筛选 + 跳转功能 计划

### 1. CommentMapper.xml — 所有 SELECT 查询增加 targetName
- 修改 `findAll` / `findByUserId` / `findByTarget` 三个查询
- 通过 LEFT JOIN 四个目标表 + COALESCE 获取目标名称
- resultMap 中添加 `targetName` 映射

### 2. PersonalCenterServlet.java — JSON 中包含 targetName
- `handleMyComments()` 的 JSON 输出中添加 `targetName` 和 `targetId` 字段

### 3. Admin-comment.jsp — 管理员端改造
- 表格上方添加分类筛选按钮（全部/景区/酒店/攻略/特产）
- 表格新增「评论对象」列，显示目标名称
- 新增「查看」按钮，跳转到对应详情页
- 客户端 JS 筛选逻辑

### 4. PersonalCenter.jsp — 用户端改造
- "我的评论"标签页添加上方分类筛选按钮
- 每条评论显示目标名称 + 「查看」跳转按钮
- 客户端 JS 筛选逻辑

### 跳转规则：
| targetType | 详情页 |
|---|---|
| SCENIC | `ScenicService-detail.jsp?id=` |
| HOTEL | `Hotel-detail.jsp?id=` |
| GUIDE | `TravelGuide-detail.jsp?id=` |
| SPECIALTY | `Specialty-detail.jsp?id=` |

### 同步 out/artifacts 对应文件